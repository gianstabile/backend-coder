<div class="product-details">
  <img src="{{this.thumbnails}}" alt="{{this.title}}" />
  <h1>{{this.title}}</h1>
  <p>{{this.description}}</p>
  <p>Price: ${{this.price}}</p>
  <button class="btn btn-primary" onclick="addToCart('{{this._id}}')">Add to
    Cart</button>
</div>